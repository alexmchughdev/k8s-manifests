apiVersion: v1
kind: Service
metadata:
  name: dfx-tag-generator
  namespace: dfx
spec:
  type: NodePort
  selector:
    app: dfx-tag-generator
  ports:
    - port: 9092
      targetPort: 9092
      nodePort: 30092
